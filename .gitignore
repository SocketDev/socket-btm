# ==============================================================================
# Build Artifacts
# ==============================================================================

# All build artifacts are gitignored but used differently:
#
# - build/dev/                : Dev build workspace (isolated for concurrent builds)
#   - build/dev/node-source/  : Dev Node.js source code
#   - build/dev/out/          : Dev build outputs (Release, Stripped, Compressed, Final)
#   - build/dev/cache/        : Dev compiled binary cache + cache-validation.hash
#   - build/dev/build-checkpoint : Dev build resume checkpoint
#
# - build/prod/               : Prod build workspace (isolated for concurrent builds)
#   - build/prod/node-source/ : Prod Node.js source code
#   - build/prod/out/         : Prod build outputs (Release, Stripped, Compressed, Final)
#   - build/prod/cache/       : Prod compiled binary cache + cache-validation.hash
#   - build/prod/build-checkpoint : Prod build resume checkpoint
#
# - build/.release-temp/      : Temporary release packaging directory
#                               (gitignored, created during release)
#
# CI Workflow:
#   1. Build jobs: Upload build/{dev,prod}/out/Final/node as GitHub Actions artifacts
#   2. Release job: Download artifacts to build/{dev,prod}/cache/node-{platform}-{arch}
#   3. Release job: Run scripts/release.mjs to package and publish
#
# Local Workflow:
#   1. First build: Compile and cache to build/{dev,prod}/cache/node-{platform}-{arch}
#   2. Subsequent builds: Restore from cache if source unchanged
#   3. Concurrent builds: Dev and prod builds fully isolated in separate directories
#
**/build/

# Distribution binaries (E2E test artifacts, regenerated on each build)
**/dist/

# Rust build artifacts
**/target/
**/Cargo.lock

# Non-pnpm lockfiles — this is a pnpm workspace; the root pnpm-lock.yaml
# is the only lockfile that should be tracked.
**/package-lock.json
**/yarn.lock

# Zig build artifacts
**/.zig-cache/

# Docker build output (from Dockerfile.local-dev)
build-output/

# Profiling artifacts (performance testing)
profiling/

# External source copies (build-time generated from monorepo packages).
packages/node-smol-builder/additions/source-patched/src/socketsecurity/bin-infra/
packages/node-smol-builder/additions/source-patched/src/socketsecurity/binject/
packages/node-smol-builder/additions/source-patched/src/socketsecurity/build-infra/
packages/node-smol-builder/additions/source-patched/src/socketsecurity/temporal/
packages/node-smol-builder/additions/source-patched/include/temporal_rs/

# Vendored npm packages (sync-time generated from node_modules).
packages/node-smol-builder/additions/source-patched/deps/fast-webstreams/

# WPT streams tests — managed as a sparse-checkout submodule at
# packages/node-smol-builder/test/fixtures/wpt/streams.
# scripts/git-partial-submodule.mts reads sparse-checkout = streams/
# from .gitmodules. The streams/ working tree is tracked through the
# submodule machinery, not ignored.
packages/node-smol-builder/additions/source-patched/deps/lzfse/src/
packages/node-smol-builder/additions/source-patched/deps/libdeflate/*
!packages/node-smol-builder/additions/source-patched/deps/libdeflate/libdeflate.gyp
# QUIC + HTTP/3 + layout source copied from lsquic-infra / yoga-layout-builder
# upstream at build time, plus the auto-generated *.gypi source manifests
# prepare-external-sources.mts emits next to them (one per vendored dep).
packages/node-smol-builder/additions/source-patched/deps/lsquic/
packages/node-smol-builder/additions/source-patched/deps/ls-qpack/
packages/node-smol-builder/additions/source-patched/deps/yoga/
packages/node-smol-builder/additions/source-patched/deps/lsquic.gypi
packages/node-smol-builder/additions/source-patched/deps/ls-qpack.gypi
packages/node-smol-builder/additions/source-patched/deps/yoga.gypi

# Firewall test/debug Dockerfiles and scripts
Dockerfile.firewall-*-test
test-firewall-*.sh

# ==============================================================================
# Dependencies
# ==============================================================================

**/node_modules/

# ==============================================================================
# Caches
# ==============================================================================

# Defensive — Node compile-cache, corepack, and other tools occasionally
# drop scratch dirs into a project-local .cache/. Nothing in this repo
# writes there intentionally; node_modules/.cache/ is the canonical home.
**/.cache/

# ==============================================================================
# Logs
# ==============================================================================

**/*.log

# ==============================================================================
# Generated Files
# ==============================================================================


# ==============================================================================
# Platform-Specific
# ==============================================================================

# macOS
**/.DS_Store
**/.AppleDouble
**/.LSOverride

# Windows
**/Thumbs.db
**/ehthumbs.db
**/Desktop.ini

# Linux
**/.directory

# ==============================================================================
# Editor Files
# ==============================================================================

# Vim
**/*.swp
**/*.swo
**/*~

# VSCode
**/.vscode/
**/*.code-workspace

# IntelliJ IDEA
**/.idea/
**/*.iml

# Emacs
# (**/*~ already covered above under Vim; keeping only Emacs-specific here)
**/\#*\#
**/.\#*

# ==============================================================================
# Environment
# ==============================================================================

**/.env
**/.env.local
**/.env.*.local

# ==============================================================================
# Python
# ==============================================================================

# Byte-compiled / optimized / DLL files
**/__pycache__/
**/*.py[cod]
**/*$py.class
**/*.so

# Distribution / packaging
# (**/dist/ and **/build/ already covered under Build Artifacts at top)
**/*.egg-info/

# Virtual environments
**/.venv/
**/venv/
**/ENV/
**/env/

# Testing and linting
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/
**/.coverage
**/coverage/
**/.tox/
**/*.gcov
**/*.gcda
**/*.gcno

# ==============================================================================
# Temporary Files
# ==============================================================================

**/tmp/
**/temp/
**/*.tmp
**/*.backup*

# Quality-scan / audit output (timestamped reports written by skills).
/reports/

# Patch backup directories (created by regenerating-node-patches skill)
**/patches/source-patched.backup-*/

# Claude Code directory (except skills and commands)
/.claude/*
!/.claude/agents/
!/.claude/commands/
!/.claude/ops/
!/.claude/rules/
!/.claude/skills/
!/.claude/hooks/
!/.claude/settings.json

# Archive directories
**/docs/archive/

# ==============================================================================
# Upstream Source Code
# ==============================================================================

# Git submodule checkouts for third-party library sources
**/upstream/

# ==============================================================================
# Build / Compiler Caches
# ==============================================================================

.ccache/

# Ephemeral Docker build context staging. Workflows `cp` the
# socket-registry tool-checksums file here before `depot buildx build`
# so Dockerfiles can resolve pnpm via `COPY .build-context/...`. Never
# committed.
.build-context/

# ==============================================================================
# Guarded File Names (intentionally never committed)
# ==============================================================================

# Guard against accidentally committing a top-level ARCHITECTURE.md.
# Project architecture docs live under docs/ (see feedback_docs_placement).
ARCHITECTURE.md

# ─── BEGIN fleet-canonical (managed by socket-wheelhouse sync) ──
# Managed by socket-wheelhouse. Don't edit locally — edit upstream
# in scripts/sync-scaffolding/checks/gitignore-fleet-block.mts and
# re-cascade via `pnpm run sync`. Project-specific ignores stay
# OUTSIDE this block; the fixer preserves them.
# Per-machine Claude Code permission config + log dirs stay ignored;
# the cascaded subdirs (agents, commands, hooks, settings.json, skills,
# workflows) are explicitly re-included so the wheelhouse cascade can
# ship them.
/.claude/*
!/.claude/agents/
!/.claude/commands/
!/.claude/hooks/
!/.claude/settings.json
!/.claude/skills/
!/.claude/workflows/

# OS noise
.DS_Store
._.DS_Store
Thumbs.db

# Build outputs — universal across the fleet. Project-specific
# variants (e.g. a non-standard dist path) go OUTSIDE this block.
**/build/
**/coverage/
**/dist/
**/.cache/

# Node
node_modules/
npm-debug.log
pnpm-debug.log
*.tgz
# ─── END fleet-canonical ────────────────────────────────────────
