/**
 * Paths used by common scripts.
 *
 * Re-exports infrastructure paths from root paths.mjs.
 * This ensures changes to paths.mjs are tracked in cache keys.
 */

export { PACKAGE_ROOT, BUILD_ROOT, SUBMODULE_PATH } from '../../paths.mjs'
