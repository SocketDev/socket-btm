# @node-smol/ai native binding

This platform-specific package contains the native backend for `@node-smol/ai`.
It is installed automatically as an optional dependency; install and use
`@node-smol/ai` instead of depending on this package directly.
