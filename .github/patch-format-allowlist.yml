# Patch format allowlist for check-patch-format.mts.
#
# Each entry opts a specific patch file out of a specific format rule.
# Entries require a `reason` field so future readers can audit whether
# the exception is still valid — when it's not, the entry (and the
# underlying patch filename/content) should be updated, not the rule
# loosened.
#
# Format:
#   - file: <path relative to repo root>
#     rule: <rule id from scripts/check-patch-format.mts>
#     reason: <one-paragraph justification>

- file: packages/node-smol-builder/patches/source-patched/016-http-server-wire.patch
  rule: numbered-series-gap
  reason: "Patch 015 was intentionally removed during R17 (see earlier quality-loop notes). Renumbering 016-022 down to 015-021 would rewrite every subsequent patch filename, break external references in skill/reference docs, and provide no functional benefit — the build applies patches in filename-sort order, which is insensitive to gaps. Keep the gap and document it here so check-patch-format.mts doesn't flag it every CI run."

# ─── multiple-patches-per-file: pre-existing convention violations ─────
#
# CLAUDE.md "Patch Rules" requires one patch per source file across the
# entire patch series. The `multiple-patches-per-file` rule enforces this
# at CI time. The entries below grandfather pre-existing violations while
# we plan a cleanup: smol-power (023-026), smol-util (027-030), and
# smol-primordials (031-034) each have 4 sibling patches that touch the
# same 4 source files (src/node_binding.cc, lib/internal/bootstrap/realm.js,
# src/node_external_reference.h, node.gyp).
#
# The right cleanup is to fold all per-binding patches into the canonical
# multi-binding patches that already exist (018-smol-builtin-bindings,
# 003-realm-smol-bindings, 020-smol-external-refs, 004-node-gyp-smol-sources).
# Tracked separately — not in this commit because it touches landed work.
#
# When the cleanup lands, delete these allowlist entries.

- file: packages/node-smol-builder/patches/source-patched/021-glibc-compat-layer.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: 021 modifies a file already touched by an earlier patch. Cleanup deferred to the broader smol-* per-binding-patch fold-in."

- file: packages/node-smol-builder/patches/source-patched/023-smol-power-binding.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-power's per-binding-patch series (023-026) follows the same per-binding-patch shape as smol-util (027-030) and smol-primordials (031-034). Cleanup is to fold all of these into 018/003/020/004."

- file: packages/node-smol-builder/patches/source-patched/024-smol-power-realm.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-power per-binding-patch series. See 023's reason."

- file: packages/node-smol-builder/patches/source-patched/025-smol-power-external-refs.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-power per-binding-patch series. See 023's reason."

- file: packages/node-smol-builder/patches/source-patched/026-smol-power-node-gyp.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-power per-binding-patch series. See 023's reason."

- file: packages/node-smol-builder/patches/source-patched/027-smol-util-binding.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-util per-binding-patch series matches smol-power's shape (the established but unenforced pattern). Cleanup deferred."

- file: packages/node-smol-builder/patches/source-patched/028-smol-util-realm.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-util per-binding-patch series. See 027's reason."

- file: packages/node-smol-builder/patches/source-patched/029-smol-util-external-refs.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-util per-binding-patch series. See 027's reason."

- file: packages/node-smol-builder/patches/source-patched/030-smol-util-node-gyp.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-util per-binding-patch series. See 027's reason."

- file: packages/node-smol-builder/patches/source-patched/031-smol-primordials-binding.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-primordials per-binding-patch series matches smol-util/smol-power's shape. Cleanup deferred to the broader fold-in."

- file: packages/node-smol-builder/patches/source-patched/032-smol-primordials-realm.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-primordials per-binding-patch series. See 031's reason."

- file: packages/node-smol-builder/patches/source-patched/033-smol-primordials-external-refs.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-primordials per-binding-patch series. See 031's reason."

- file: packages/node-smol-builder/patches/source-patched/034-smol-primordials-node-gyp.patch
  rule: multiple-patches-per-file
  reason: "Pre-existing: smol-primordials per-binding-patch series. See 031's reason."
