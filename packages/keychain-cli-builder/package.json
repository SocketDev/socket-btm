{
  "name": "keychain-cli-builder",
  "version": "0.0.0",
  "private": true,
  "description": "Builds socket-keychain, a standalone C++ CLI for the operating-system credential store",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mts",
    "clean": "node scripts/clean.mts",
    "cover": "vitest run --coverage",
    "test": "vitest run"
  },
  "dependencies": {
    "@socketsecurity/lib-stable": "catalog:",
    "build-infra": "workspace:*",
    "keystore-infra": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "vitest": "catalog:"
  }
}
