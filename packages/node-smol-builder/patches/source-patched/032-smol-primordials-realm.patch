# @node-versions: v25.9.0
# @description: Reserve `smol-primordials` for the node: scheme
#
# Adds `smol-primordials` to schemelessBlockList so consumers must
# `require('node:smol-primordials')` (matching every other smol-*
# module).
#
# Sequencing: 032 anchors after 028 (smol-util realm). Insert keeps
# alphabetic order: smol-power < smol-primordials < smol-purl.
#
# Files modified:
# - lib/internal/bootstrap/realm.js: schemelessBlockList entry
#
--- a/lib/internal/bootstrap/realm.js
+++ b/lib/internal/bootstrap/realm.js
@@ -135,3 +135,4 @@
   'smol-power',
+  'smol-primordials',
   'smol-purl',
   'smol-sql',
