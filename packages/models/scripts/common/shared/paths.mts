/**
 * Paths used by common scripts.
 *
 * Re-exports infrastructure paths from root paths.mts.
 * This ensures changes to paths.mts are tracked in cache keys.
 */

export { PACKAGE_ROOT, BUILD_ROOT } from '../../paths.mts'
