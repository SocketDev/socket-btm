# @node-versions: v25.9.0
# @description: Wire smol-primordials native source into node.gyp
#
# Adds src/socketsecurity/primordials/primordials_binding.cc to the
# main node target. Pure platform-agnostic V8 binding glue — no per-OS
# variants needed.
#
# Sequencing: 034 anchors after 030 (smol-util node-gyp). The hunk
# inserts the primordials block immediately after the util block.
#
# Files modified:
# - node.gyp: add smol-primordials binding source
#
--- a/node.gyp
+++ b/node.gyp
@@ -1,5 +1,12 @@
         ['1==1', {
           'sources': [
             'src/socketsecurity/util/util_binding.cc',
           ],
         }],
+        # Socket Security: smol-primordials binding. Pure V8 glue,
+        # platform-agnostic.
+        ['1==1', {
+          'sources': [
+            'src/socketsecurity/primordials/primordials_binding.cc',
+          ],
+        }],
