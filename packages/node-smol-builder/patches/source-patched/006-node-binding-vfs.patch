Socket Security: Replace wasi binding with smol_vfs

This patch replaces the WASI binding with Socket Security's VFS binding
in the built-in binding list.

Files modified:
- src/node_binding.cc: Replace wasi binding with smol_vfs

--- a/src/node_binding.cc
+++ b/src/node_binding.cc
@@ -92,7 +92,8 @@
   V(util)                                                                      \
   V(uv)                                                                        \
   V(v8)                                                                        \
-  V(wasi)                                                                      \
+  /* V(wasi) */                                                                \
+  V(smol_vfs)                                                                  \
   V(wasm_web_api)                                                              \
   V(watchdog)                                                                  \
   V(worker)                                                                    \
