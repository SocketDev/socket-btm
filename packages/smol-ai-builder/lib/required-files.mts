/**
 * Required-files manifest for a smol-ai install.
 *
 * The builder's released artifact is a single native addon file. A valid
 * install must contain the addon at the canonical output name.
 */
export const SMOL_AI_REQUIRED_FILES = ['smol_ai.node']
