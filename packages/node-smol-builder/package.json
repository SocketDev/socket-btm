{
  "dependencies": {
    "build-infra": "workspace:*",
    "@socketsecurity/lib": "catalog:",
    "yoctocolors-cjs": "catalog:"
  },
  "description": "Custom Node.js binary builder with Socket security patches",
  "devDependencies": {
    "vitest": "catalog:"
  },
  "license": "MIT",
  "name": "node-smol-builder",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:all": "node scripts/build.mjs --all-platforms",
    "clean": "del-cli build dist .cache additions/002-bootstrap-loader/internal/socketsecurity_bootstrap_loader.js",
    "clean:build": "del-cli build",
    "clean:dist": "del-cli dist",
    "release": "node scripts/release.mjs",
    "release:publish": "node scripts/release.mjs --publish",
    "release:dry-run": "node scripts/release.mjs --dry-run",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "version": "1.2.0"
}
