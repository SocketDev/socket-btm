{
  "name": "curl-builder",
  "version": "0.0.0",
  "private": true,
  "description": "Builds libcurl with mbedTLS for HTTPS support in stub binaries",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./lib/ensure-curl": "./lib/ensure-curl.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@socketsecurity/lib": "catalog:",
    "build-infra": "workspace:^0.0.0",
    "vitest": "catalog:"
  }
}
